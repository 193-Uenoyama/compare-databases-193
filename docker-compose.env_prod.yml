version: '3.7'

services:
  node-server:
    environment:
      NODE_ENV: production
    command: [ "node", "dist/express-src/app.js" ]
    restart: always

  node-client:
    environment:
      NODE_ENV: production
    command: [ "sh" ]
    restart: always
